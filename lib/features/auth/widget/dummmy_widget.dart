import 'package:flutter/material.dart';

class DummmyWidget extends StatelessWidget {
  const DummmyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
